# Jetson Nano full voice pipeline — deps.
#
# The single source of truth for the Nano is requirements/jetson.txt (floored
# for JetPack 4.6 / Python 3.8 / aarch64, and already includes faster-whisper,
# the mic stack, ONNX Runtime, and the webrtcvad gate). Install that:
#
#     pip install -r requirements/jetson.txt
#
# ⚠️ On the Jetson, replace the CPU `onnxruntime` floor in jetson.txt with
# NVIDIA's `onnxruntime-gpu` wheel to bind CUDAExecutionProvider (see the note
# in jetson.txt and nvidia/NvidiaRun.md). Piper TTS is an external binary, not a
# pip dep — see docs/STT-TTS.md Part 3 / NvidiaRun.md.

-r ../requirements/jetson.txt
