# Dev-box dependencies: MarianMT fine-tune + CT2/ONNX export + benchmarks.
# (Run on an x86/modern box, NOT the Jetson — the Jetson uses jetson.txt.)
#
# Install CUDA torch SEPARATELY if you have an NVIDIA GPU:
#   pip uninstall -y torch
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then:
#   pip install -r requirements/dev.txt
#   pip install -e .

transformers
sentencepiece
sacremoses
protobuf
huggingface_hub
ctranslate2

# Fine-tune
datasets
accelerate
evaluate
sacrebleu
psutil

# ONNX export + int8 quantization for the Jetson (jetsonNvidia.md §6)
optimum[onnxruntime]
onnx
onnxruntime

# torch is intentionally not pinned here — install the CUDA (or CPU) wheel
# matching your machine first (see README).
