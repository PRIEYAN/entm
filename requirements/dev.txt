# Dev-laptop dependencies: MarianMT fine-tune + CT2 convert + benchmarks.
#
# Install CUDA torch SEPARATELY if you have an NVIDIA GPU:
#   pip uninstall -y torch
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Then:
#   pip install -r requirements/dev.txt
#   pip install -e .

transformers
sentencepiece
sacremoses
protobuf
huggingface_hub
ctranslate2
fastapi
uvicorn[standard]

# Fine-tune
datasets
accelerate
evaluate
sacrebleu
psutil

# torch is intentionally not pinned here — install the CUDA (or CPU) wheel
# matching your machine first (see README).
