# Deps for the GPU LAPTOP side of the voice pipeline (STT + mic + SSH client).
# Runs locally: faster-whisper is a CTranslate2 build of Whisper, GPU-accelerated.
# Offline after the Whisper model downloads once (cached to ~/.cache).
#
#   pip install -r requirements/laptop.txt
#
# For GPU (CUDA) Whisper you need a CUDA-enabled environment; faster-whisper uses
# the ctranslate2 CUDA runtime. CPU also works (slower) via compute_type="int8".
faster-whisper
sounddevice
soundfile
